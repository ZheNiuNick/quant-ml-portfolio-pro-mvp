# Nixpacks configuration for Railway deployment
# Note: Git LFS files are not downloaded during build
# Large files (factor_store.parquet) are downloaded from Hugging Face on-demand

[phases.build]
cmds = [
    "pip install -r zhe_trading_strategy/requirements.txt"
]

